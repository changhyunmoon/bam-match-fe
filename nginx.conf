server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # API 요청을 백엔드로 프록시하고 싶다면 여기에 추가 설정을 할 수 있습니다.
    # 하지만 현재는 프론트엔드 정적 배포에 집중합니다.
}